"""
Test suite for Sora CLI.

Run tests with: uv run pytest
Run with coverage: uv run pytest --cov=sora --cov-report=html
Run without integration tests: uv run pytest -m "not integration"
Run smoke tests: uv run pytest -m smoke
"""
